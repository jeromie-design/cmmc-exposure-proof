# No environment variables required for basic operation.
# The app uses only public APIs (crt.sh) and direct HTTP probing.
#
# Optional future config:
# NEXT_PUBLIC_SITE_URL=https://exposure.cinderlabs.ai
# RATE_LIMIT_MAX=10
